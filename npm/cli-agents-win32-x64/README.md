# @cueframe/cli-agents

Platform-specific binary package. You should install the main package instead:

```bash
npm install -g @cueframe/cli-agents
```

See [@cueframe/cli-agents](https://www.npmjs.com/package/@cueframe/cli-agents) for documentation.
